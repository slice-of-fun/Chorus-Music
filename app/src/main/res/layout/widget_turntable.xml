<?xml version="1.0" encoding="utf-8"?>
<!-- Turntable Widget - Rebuilt as a Floating Minimalist Vinyl -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
>

    <!-- Floating Circular Album Art (The "Vinyl") -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:background="@drawable/widget_art_circle_bg"
android:elevation="8dp">
        
        <ImageView
            android:id="@+id/widget_turntable_album_art"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/widget_turntable_default_art"
            android:contentDescription="@string/album_art" />
            
        <!-- Subtle dark gradient overlay at the bottom for floating controls -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/widget_bottom_gradient"
            android:alpha="0.8" />
            
        <!-- Floating Minimalist Controls Container inside the vinyl -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="16dp"
            android:orientation="horizontal"
            android:background="@drawable/widget_pill_bg"
            android:padding="8dp"
            android:gravity="center">

            <!-- Previous Button -->
            <ImageButton
                android:id="@+id/widget_turntable_prev_button"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:padding="8dp"
                android:scaleType="fitCenter"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_widget_skip_previous"
                android:contentDescription="@string/previous" />

            <!-- Play Button -->
            <ImageView
                android:id="@+id/widget_turntable_play_pause"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="8dp"
                android:padding="6dp"
                android:src="@drawable/ic_widget_play"
                android:contentDescription="@string/play_pause" />

            <!-- Next Button -->
            <ImageButton
                android:id="@+id/widget_turntable_next_button"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:padding="8dp"
                android:scaleType="fitCenter"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_widget_skip_next"
                android:contentDescription="@string/next" />

        </LinearLayout>
    </FrameLayout>
</FrameLayout>
